# Personal Task Manager API

A FastAPI-based Task Manager API using PostgreSQL and Docker.

## Features

- Create tasks
- List tasks
- Update tasks
- Delete tasks
- Mark tasks as complete

## Run with Docker

```bash
docker compose up --build
```
